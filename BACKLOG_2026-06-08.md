# FitterWelderPro — Consolidated Audit Backlog (2026-06-08)

Round 1 (2026-06-07, 50 iter) + Round 2 (2026-06-08, 50 iter).
Round 1 had 67 items; 5 P0 shipped today (commit d025247).

Round 2 produced ~150 raw findings across 50 iterations. After dedup vs round 1 and severity bucketing this added: 14 net-new P0 items (incl. the regression-risk subsection), 36 net-new P1 items, 28 net-new P2 items, 22 net-new P3 items. Round 1 items that overlap are merged with their round-2 refs noted in `Refs`.

## ✓ Shipped today (5 P0 items)

- ~~P0-01 — Guard weld journal load/save/delete against disposed-state crashes~~ (DONE — d025247)
- ~~P0-02 — Stop swallowing real SQLite errors in `_ensureTable` migrations~~ (DONE — d025247)
- ~~P0-03 — Block / warn on negative or out-of-range CUT values in cut list totals~~ (DONE — d025247)
- ~~P0-04 — Restore previous device id / add grace period before stripping PRO~~ (DONE — d025247)
- ~~P0-05 — Stop stale results from being copied after validation failure~~ (DONE — d025247)

Round 2 verified each fix with a dedicated regression-check lens and flagged residual gaps below ("P0 regression-risk" subsection).

---

## P0 — Ship-blocker / data loss / crash risk

### P0 regression-risk (gaps in today's d025247 fixes)

#### P0r-01 — Apply P0-03 totals/clamp guards to iso_notebook_screen (sister screen)
- **What**: Mirror the P0-03 pattern in iso_notebook: in `_CutCalc.cutMm`/`_totalMm`/PDF watermark `total`, gate on `v.isFinite && v >= 0`; clamp manual ISO entries to (0, 100000) mm and show "Sprawdź wymiar" hint above ~100 m; surface `_invalidSegmentCount` on the summary bar AND on the PDF watermark ("N do sprawdzenia" red chip).
- **Where**: `lib/screens/iso_notebook_screen.dart:75-95, 1351, 2898-2942, 2924, 3048-3052, 4738-4747`
- **Why**: P0-03 patched only `iso_scanner_screen`. The notebook still poisons the headline CUT, clipboard summary, PDF cut-list and stick-nesting hint from negative or absurd ISO values. A welder copying the PDF to the saw is short on stock — the exact symptom P0-03 set out to eliminate, leaking through the sister screen.
- **Effort**: M
- **Refs**: round-1 P0-03; round-2 Iter #1 (high + med)

#### P0r-02 — Plug the P0-03 string-laundered AI clamp bypass
- **What**: When the AI clamp rejects `dim` in `_applyAiResult`, do NOT auto-fill `seg.iso.text` from `aiSeg.rawDimension!`. Either leave blank with a "Sprawdź wymiar" chip on that row, or try-parse the raw and re-apply the (0, 100000) bound before assigning.
- **Where**: `lib/screens/iso_scanner_screen.dart:262-267`
- **Why**: The fallback re-routes the same hallucinated number through `rawDimension` as a string; the clamp is bypassed and `parseIsoExpression` quietly accepts the absurd value.
- **Effort**: S
- **Refs**: round-1 P0-03; round-2 Iter #2 (high)

#### P0r-03 — Surface empty-ISO + populated-deduct rows in `_invalidSegmentCount`
- **What**: Drop the early `continue` on empty ISO in `_invalidSegmentCount`, or: `if (s.iso.text.trim().isEmpty && s.deducts.every((d) => d.value.text.trim().isEmpty)) continue;`
- **Where**: `lib/screens/iso_scanner_screen.dart:594-602`
- **Why**: Today, an ISO-empty + deducts-populated row is silently excluded from total AND from the "do sprawdzenia" counter; welder walks to the saw with the cut-list missing an entire row.
- **Effort**: S
- **Refs**: round-1 P0-03; round-2 Iter #2 (high)

#### P0r-04 — Track per-deduct parse failures (sister of P0-03 ISO-side guard)
- **What**: Bump `_invalidSegmentCount` (or surface per-row red dot) whenever `parseIsoExpression(d.value.text)` throws inside `cutMm`; set `errorText` on the offending deduct's `TextField` (currently set only for out-of-range, not parse failure).
- **Where**: `lib/screens/iso_scanner_screen.dart:75-82, 79-80 (silent `catch (_)` swallow)`
- **Why**: Deduct typed "abc"/"76 1/2" silently drops, `cutMm` looks plausible, `_invalidSegmentCount` stays 0 → welder cuts pipe stock LONGER than needed (waste) or the negative-cut chip never fires.
- **Effort**: M
- **Refs**: round-1 P0-03; round-2 Iter #2 (med)

#### P0r-05 — Fix P0-05 stale-IconButton enable-state on validation early-return
- **What**: Move/duplicate `setState(() {});` immediately after the four `.clear()` calls (before any validation early-return), or wrap the four clears inside a `setState`. Belt-and-braces: in `copyToClipboard` early-return + warn snackbar when `value.trim().isEmpty`.
- **Where**: `lib/screens/rolling_offset_screen.dart:46-49, 57-81, 305-311`
- **Why**: P0-05 wipes the four result controllers BEFORE the early-return at lines 74 and 80 but does not trigger a rebuild on those paths; the copy IconButton stays "enabled" against now-empty controllers. Tap-copy silently overwrites the clipboard's previous 707.1 with an empty string — different wrong-cut path, equally damaging.
- **Effort**: S
- **Refs**: round-1 P0-05; round-2 Iter #7, Iter #37 (high)

#### P0r-06 — Clear result controllers on angle-chip change (rolling offset)
- **What**: In `_angleBtn.onTap`, additionally clear `_trueOffsetController`, `_multiplierController`, `_travelController`, `_runController`; if new value != 'custom' also clear `_customAngleController`. Same wipe must run on `_selectedAngle == 'custom'` mount/unmount. Optionally annotate WYNIKI header with the angle used for the last calc.
- **Where**: `lib/screens/rolling_offset_screen.dart:46-49, 53-55, 76-81, 245-269`
- **Why**: P0-05 only covers the re-CALCULATE path. Tapping a different angle preset post-calc leaves the 45° Travel=707.1 visible while the chip jumps to 60°. Welder copies 707.1 thinking it's the 60° value — wrong cut by ~130 mm on a Schedule 80 spool.
- **Effort**: S
- **Refs**: round-1 P0-05, P3-12; round-2 Iter #7 finding #2, Iter #37 (high)

#### P0r-07 — Apply P0-02 selective-catch policy to iso_notebook silent `catch (_)` reservoir
- **What**: Narrow each `catch (_)` to the actually-expected exception type; log others via `debugPrint` with a tag so they surface in CI/Sentry; promote the `ComponentClassification.isPhysical` catches to rethrow or assert.
- **Where**: `lib/screens/iso_notebook_screen.dart:199-203, 224-228, 1353, 1359, 2899, 2925, 3643, 6613-6618` (12+ silent catches)
- **Why**: P0-02 declared the policy "only swallow what we expect". `ComponentClassification.isPhysical` throwing is a data-integrity issue — a silently-skipped physical component never deducts from CUT. The notebook is the next-largest reservoir of the same antipattern P0-02 fixed in `_ensureTable`.
- **Effort**: M
- **Refs**: round-1 P0-02; round-2 Iter #1 (low, but data-integrity class)

#### P0r-08 — Harden P0-02 duplicate-column detection against localized SQLite messages
- **What**: Match on `DatabaseException.isDuplicateColumnError()` (sqflite ≥ 2.3) or substring `"code 1" + "column"`; OR pre-check `PRAGMA table_info(weld_journal)` and only run ALTER for missing columns (idempotent, no exception channel at all).
- **Where**: `lib/screens/weld_journal_screen.dart:148-153`
- **Why**: Today's substring match (`"duplicate column"`, `"already exists"`) breaks on Huawei/Xiaomi ROMs with localized sqlite error tables — a benign migration rethrows as fatal, blocking the journal entirely cold-start.
- **Effort**: S
- **Refs**: round-1 P0-02; round-2 Iter #3 (med)

#### P0r-09 — Stash previous status before optimistic `_cycleStatus` mutation
- **What**: `final prev = e.status; e.status = next;` BEFORE the await; rollback `e.status = prev` in the catch and `setState`.
- **Where**: `lib/screens/weld_journal_screen.dart:384-402`
- **Why**: P0-01 wrapped the DAO call in try/catch but kept `e.status = next;` BEFORE the await. On failure SnackBar fires but `_entries` holds the new (unpersisted) status — a NOK weld reads as OK on screen while DB still says PENDING. Traceability fail with no audit trail.
- **Effort**: S
- **Refs**: round-1 P0-01; round-2 Iter #3 (high)

#### P0r-10 — Harden P0-04 grace state machine against permanently-flaky backends
- **What**: Call `_clearPendingDowngrade()` on every local "purchase succeeded" event (not just on subsequent "active" backend read); require 3 confirming reads (not 2) when device is online but backend `lastSuccessAt > 5 min ago`. Add edge case: a STRUCTURAL `is_active=false` (e.g. INTERNET perm missing on sideloaded APKs) currently rips PRO 2 min after first launch with no recovery.
- **Where**: `lib/services/premium_service.dart:107-117, 217-248, 222 (_kDowngradeGrace)`
- **Why**: P0-04 protects against transient flake but cannot distinguish "transient" from "permanent" backend misconfiguration; paying users on misconfigured backend installs get downgraded mid-job.
- **Effort**: M
- **Refs**: round-1 P0-04; round-2 Iter #1 (med), Iter #27 (low)

#### P0r-11 — Add widget+unit test regression net for all 5 P0 fixes
- **What**: Add `test/widgets/weld_journal_screen_test.dart` (`_load` rethrow surfaces SnackBar, `_saving` cleared on error); `PremiumService` unit test for the downgrade-grace state machine (3 reads across 2 min); unit test on `_totalCutMm` covering negative + non-finite + clamp + `_invalidSegmentCount` increments; widget test on `RollingOffsetScreen._calculate` confirming controllers clear before early-return AND copy IconButton goes disabled.
- **Where**: `test/` (new files for journal, premium_service, iso_scanner, rolling_offset)
- **Why**: Commit d025247 says "120 tests pass" but exercises none of the new branches. Any future refactor silently re-introduces today's ship-blocker bugs. The math-integrity fixes (P0-03) MUST be pinned to tests.
- **Effort**: L
- **Refs**: round-1 P0-01..P0-05; round-2 Iter #1, Iter #2, Iter #37, Iter #40 (high)

#### P0r-12 — Selective-catch on iso_notebook five toggle persisters
- **What**: Wrap each `prefs.setBool` in try/catch; on failure revert the `setState` and surface "Nie zapisano ustawienia" SnackBar with Retry.
- **Where**: `lib/screens/iso_notebook_screen.dart:640-670 (_setHintHidden, _setShowAxisCompass, _setShowStatusBox, _setPaperMode, _setAxisLock)`
- **Why**: All five toggle persisters call `setState(...)` BEFORE awaiting `setBool` with no try/catch and no `mounted` guard. On locked/full storage the in-memory toggle flips but doesn't persist — paper-mode invisibly resets next launch. Same antipattern P0-01 ratified for weld_journal.
- **Effort**: S
- **Refs**: round-1 P0-01; round-2 Iter #1 (med)

### P0 net-new (round-2 high severity, ship-blocker class)

#### P0-06 — Confirm before wiping manually-entered segments on AI scan
- **What**: Before `_segments.clear()` in `_applyAiResult`, if any existing segment has user input, show a confirm dialog ("Zastąpić wpisane odcinki wynikiem AI?"). Independently, snapshot pre-AI segments and offer an `_undoAiResult` SnackBar action.
- **Where**: `lib/screens/iso_scanner_screen.dart:244-282, 505-517, 660-667`
- **Why**: A welder types 5 segments, taps "Analyse AI" out of curiosity → all 5 typed rows are silently wiped → after AI the PopScope's `canPop=false` prompts "Porzucić skan?" → reflex-tap Discard → ends with nothing. Net: useful data → 30-90 s Vision API spend → zero recovery.
- **Effort**: M
- **Refs**: round-2 Iter #2 (high)

#### P0-07 — Tighten `JobAddScreen` Stripe checkout against duplicate-charge
- **What**: Gate the Retry SnackBarAction with `onPressed: _saving ? null : _save`; expose constructor injection for `JobsService` so the path can be unit-tested (`final JobsService _svc = service ?? JobsService.instance;`); add widget test pumping a stalling `createCheckout` future, tapping Retry twice, asserting `createCheckoutCallCount == 1`. Add `if (!mounted) return;` before the post-await `setState(_saving=false)` at line 209.
- **Where**: `lib/screens/job_add_screen.dart:34-91, 170-222, 217-219, 104-110, 227-234`
- **Why**: Retry SnackBar fires EXACTLY in the cellular-flake window where the user mashes the button — creates duplicate Stripe Checkout sessions. Welder pays 49 PLN twice → revenue dispute. Singleton coupling means there is no DI seam to prevent the bug in CI.
- **Effort**: M
- **Refs**: round-2 Iter #5 (high × 3)

#### P0-08 — Persistent welder identity / qualification capture at registration
- **What**: Add required `welderStamp` (uppercase, regex `[A-Z0-9-]{2,12}`) + optional `wpqrNo`, `wpqrExpiry`, `certBody` (TÜV/UDT/LRS) fields on the registration screen; persist users via SharedPreferences/sqflite (`users` table) instead of an in-memory `Map<String,String>` that vanishes on app restart; FK the `weld_journal.author` column on stamp instead of email; add GDPR/RODO consent + qualification declaration checkboxes.
- **Where**: `lib/screens/register_screen.dart:30-64, 73-130`; `lib/services/auth_service.dart:13-42`
- **Why**: ISO 3834 / EN ISO 9606 / ASME IX QW-301 require a unique welder identifier on every joint. Current in-memory store creates "ghost welder" entries that an audit cannot reconcile. PED 2014/68/EU Annex I 3.1.2 needs documented qualified-personnel link. Phone reboot Monday night = Tuesday morning's traceability broken.
- **Effort**: L
- **Refs**: round-2 Iter #24 (high × 2 + med × 5)

#### P0-09 — Prefab engine: fix `centreToFace` CTE double-subtraction + add negative-cut guard
- **What**: For `DimRef.centreToFace`, decide which side is the centre (per existing physical-or-not flag or take only one CTE param), then `return isoValueMm - centreSideCte - midPhysicalSumMm;`. After the switch, `final cut = ...; return cut <= 0 ? double.nan : cut;` (sentinel for downstream warning). Add unit test `centreToFace + leftCte=55 + rightCte=55 → 445` (not 390); test `cutLengthMm(iso=100, ref:CTC, leftCte=55, rightCte=55) → isNaN`. Also guard `!isFinite` (covers infinity from `parseIsoExpression('1e500')`).
- **Where**: `lib/services/prefab_engine.dart:56-67, 59-60, 47, 75-80, 45 (midPhysicalSumMm)`
- **Why**: Today `centreToFace` subtracts `cteBoth = leftCte + rightCte` even though the rule says "face side: no body subtracted". Fitter dimensioning oś-czoło on an elbow-to-flange spool gets a CUT 55-76 mm too short → pipe arrives undersized at fit-up → scrap. Tests pass only because they manually zero the face-side CTE.
- **Effort**: M
- **Refs**: round-1 P0-03 (sister); round-2 Iter #25 (high × 2 + med + low)

#### P0-10 — Wire actual AI tutor backend (currently dead) + add code-citations
- **What**: Either (a) wire `BackendService.searchAnswer` to the PrzetargAI Railway `/api/fitterwelderpro/tutor` route with a Claude haiku prompt grounded in ASME/ISO/EN/AWS excerpts and structured JSON `{answer, citations: [{standard, clause}], formulas: []}`, OR (b) mark the Tutor entry `Beta / coming soon` in the menu and gate behind a feature flag. Also widen the scope filter token set (WPS/WPQR/PQR/NDT/HAZ/PWHT/GTAW/GMAW/SMAW/FCAW/TIG/MIG/MAG + ISO/ASME/AWS/EN/PN-EN/DIN/B31/D1.1/5817/9606/15614/9692/13920/PED) with diacritic-insensitive normalisation; add `.timeout(Duration(seconds: 20))` + per-message `_requestSeq` to discard stale responses.
- **Where**: `lib/screens/tutor_screen.dart:45-50, 45 (lower), 69-83, 69-112, 180-185`; `lib/services/backend_service.dart:40-58 (searchAnswer returns null)`
- **Why**: The Tutor is a Premium feature that returns `tutor_no_answer` on EVERY fresh question because the scope filter rejects "WPS/WPQR/PWHT/NDT/RT/UT/PT/VT/ISO/ASME/AWS/EN/PN-EN" — i.e. the exact code questions the tutor exists for — and the search backend is a stub. Worse, no timeout means a late answer to question A renders as the response to question B → welder applies the wrong undercut/preheat limit → joint fails NDT or service.
- **Effort**: L
- **Refs**: round-2 Iter #23 (high × 2 + med × 3)

#### P0-11 — HomeScreen: wrap `_load`/Navigator pushes against silent failures
- **What**: Wrap `_load` in try/catch; on error set `_loading = false`, keep last-known stats, surface `ScaffoldMessenger.showSnackBar` with PL+EN "Nie udało się wczytać projektów" + Retry. Factor `_safeNavigate(ctx, WidgetBuilder)` helper used by all 8 `Navigator.push` sites with `.catchError` showing "Nie udało się otworzyć ekranu" + Retry.
- **Where**: `lib/screens/home_screen.dart:51-68, 130-132, 166-235, 259-264`
- **Why**: Same antipattern P0-01 fixed in weld_journal. Home screen has ZERO SnackBar usage anywhere — every error path is silent. A wedged spinner on cold start with stale numbers and no Retry CTA looks like a bricked app. Pixel 4a + 2 GB free + SKANER ISO + denied camera permission = silent pop back to home, welder uninstalls.
- **Effort**: M
- **Refs**: round-2 Iter #21 (high × 2)

#### P0-12 — Persist pre-weld checklist state + add audit trail
- **What**: Persist `_done` (as `Set<String>` keyed by stable check id, not list index) + `_material.key` to Hive/SharedPreferences keyed by current job/weld number (or timestamp); POST snapshot `{weldId, materialKey, checkedItems[], completedAt, welderId}` to `/api/fitterwelderpro/checklists` on `all == true`; degrade to local-only when offline and replay on reconnect. On material change, `_done.removeWhere((i) => i >= _checks.length)` or move to ID-keyed Set so ticks don't drift to different items on P-No change.
- **Where**: `lib/screens/pre_weld_checklist_screen.dart:14-18, 88-150, 136-138, 152-162, 164-171, 184-188`
- **Why**: ZERO persistence + ZERO audit trail on a safety-critical checklist. Welder ticks 8/15 items, gets called for an argon bottle, comes back → empty list → re-ticks from memory (skip = defect). After background-kill, the route restores with zero ticks and "generic" material → no visible cue → welder strikes arc believing checklist was completed. Worse: P-No switch (1 → 10 Duplex) leaves indices 11-12 pre-ticked under their new Duplex-critical meaning ("Interpass MAX 150°C") — false-affirms safety check.
- **Effort**: L
- **Refs**: round-1 P1-25; round-2 Iter #11 (med), Iter #41 (high × 2 + med × 2)

#### P0-13 — Round-trip data corruption from PL/EN decimal-separator + clipboard interop
- **What**: Either (a) use `context.language == AppLanguage.en` to localise the separator (subscribes the widget to rebuilds at calc-time per active locale), and when locale changes since last calc re-format existing result controllers via a stored `double` cache; OR (b) store results as canonical `double` and format on display only. On the copy path, always pass an unambiguous form (`replaceAll(',', '.')` for clipboard) while leaving the visible field locale-formatted.
- **Where**: `lib/screens/pipe_route_calculator_screen.dart:92-96, 333-342`
- **Why**: Commit 69310f8 read `AppLanguageController.isEnglish` (static) outside a build context → after a CALC + language toggle the result controllers keep PL comma. The copy IconButton hands `1234,5` to an EN-locale spreadsheet which imports as `12345` → off by an order of magnitude in spool fabrication. A four-figure scrap cost from a "translation polish" commit.
- **Effort**: M
- **Refs**: round-1 P1-22; round-2 Iter #39 (high × 2)

#### P0-14 — Pre-weld checklist destructive Reset has no confirm + no undo
- **What**: Wrap Reset in AlertDialog confirm ("Wyzerować checklistę? — N pozycji zaznaczonych" / "Reset checklist? — N items ticked"), call `Haptic.error()` after the wipe, snapshot the previous `_done` set into a local for a 5-6 s SnackBar `Cofnij` / `Undo` action.
- **Where**: `lib/screens/pre_weld_checklist_screen.dart:184-187`
- **Why**: Phantom thumb tap during scroll wipes 60-90 s of careful inspection silently, no haptic, no undo. Welder may strike arc believing the checklist was completed when it was reset to zero by the same finger gesture used to scroll. Round-1 P1-05 covered destructive confirm; this screen was not in scope.
- **Effort**: S
- **Refs**: round-1 P1-05, P1-09; round-2 Iter #11 (low — flagged here because of safety semantics)

---

## P1 — High impact, low effort (do first)

### Round-1 P1 items still applicable (copied verbatim, refs extended)

#### P1-01 — Make `_load` mounted-safe everywhere (jobs, material list, weld journal)
- **What**: Add `if (!mounted) return;` guards after every `await` in `_load`/`_addNew` polling/`_openEditor` etc., and wrap in try/catch so failure resets `_loading=false` + shows a SnackBar with Retry.
- **Where**: `lib/screens/jobs_screen.dart:50-111, 181-209`; `lib/screens/material_list_screen.dart:38-62`; `lib/screens/weld_journal_screen.dart:195-199`
- **Why**: Frozen orange spinner on slow workshop signal is the #1 "is the app broken?" symptom; users force-quit and lose state.
- **Effort**: S
- **Refs**: round-1 [3, 4, 14, 39]; round-2 Iter #14 (perf angle), Iter #43

#### P1-02 — Validate picked images before AI call (size, existence, extension)
- **What**: After `_pickImage`, validate `File(path).existsSync()`, `lengthSync() > 1024` and ≤ 8 MB, lowercased extension in {jpg,jpeg,png,webp,heic}; refuse with specific PL/EN toast and pre-shrink >8 MB images to 2048 px long edge before AI call.
- **Where**: `lib/screens/iso_scanner_screen.dart:108-176`
- **Why**: Cloud-stub photos and 12 MP HDR images burn AI tokens and Railway bandwidth on jobsite 4G.
- **Effort**: M
- **Refs**: round-1 [2]

#### P1-03 — Normalize Unicode minus / fraction / whitespace before ISO parsing
- **What**: In `parseIsoExpression` pre-pass: map U+2212, U+2010-U+2015 → `-`; map ½ → `1/2`, ¼ → `1/4`, ¾ → `3/4`; strip U+200B/U+202F/U+200E/F zero-width chars; strip trailing `mm`/`in` and a single thousand-separator (incl. U+00A0, U+202F).
- **Where**: `lib/services/iso_parser.dart` (consumed by `iso_scanner_screen.dart:1344-1418`, `elbow_takeout_screen.dart:40-47`, `pipe_route_calculator_screen.dart:27`); extend to `quick_converter_screen._parse`
- **Why**: Welders paste from PDFs/WhatsApp; smart-quote and U+2212 silently fail. Quick-converter has the same gap (Iter #45 / Iter #15).
- **Effort**: S
- **Refs**: round-1 [2, 12, 32, 34, 37]; round-2 Iter #15, Iter #45

#### P1-04 — Add explicit reset/clear all + "Wyczyść" tooltips on every calc screen
- **What**: Add `IconButton(Icons.refresh, tooltip: 'Wyczyść')` to AppBar of hydrotest, orbital_tig, heat_input, rolling_offset, pipe_route, tungsten, quick_converter — resets controllers + chip state. heat_input reset must also clear the selected material chip (no current unselect path).
- **Where**: `lib/screens/hydrotest_screen.dart:88-99`; `lib/screens/orbital_tig_screen.dart:25-77`; `lib/screens/heat_input_screen.dart:163-209`; etc.
- **Why**: Multi-job shift = 3-4 calcs back-to-back; manual field clearing in gloves is painful and inconsistent.
- **Effort**: S
- **Refs**: round-1 [8, 10, 11, 33, 35]; round-2 Iter #16

#### P1-05 — Confirm before destructive AI chat / journal clear + swap icon
- **What**: Wrap AppBar refresh/clear-chat with `showDialog` confirmation when `_messages.length > 1`. Swap `Icons.refresh` → `Icons.delete_sweep_outlined` or `Icons.clear_all` so the icon semantics match destruction.
- **Where**: `lib/screens/ai_chat_screen.dart:174-189`; `lib/screens/weld_journal_screen.dart:360-374`
- **Why**: Refresh icon means "reload" everywhere else in the app — here it means "wipe thread". Inconsistent icon semantics + no confirm = irrecoverable data loss in gloves.
- **Effort**: S
- **Refs**: round-1 [3, 19, 44]; round-2 Iter #19

#### P1-06 — ShaderMask fade-edge gradient + ">" chevron on every horizontal scroll strip
- **What**: Wrap horizontal `ListView`/`SingleChildScrollView` (iso_notebook toolbar, pre_weld checklist material picker, chat rooms, premium plan cards, help_screen filter chips) in a `ShaderMask` with linear gradient to opaque on right edge OR replace with `Wrap`.
- **Where**: `lib/screens/iso_notebook_screen.dart:4457-4482`; `lib/screens/pre_weld_checklist_screen.dart:236-272`; `lib/screens/quick_converter_screen.dart:29-37`; `lib/screens/help_screen.dart:151-173`
- **Why**: Duplex/Inconel/P91 chips hidden off-screen never gate safety checks; help categories beyond NDT/Safety undiscovered.
- **Effort**: S
- **Refs**: round-1 [1, 11, 15, 36, 40]; round-2 Iter #22

#### P1-07 — Bump chip / IconButton / send-button tap targets to ≥48 dp
- **What**: Set explicit `BoxConstraints(minWidth: 48, minHeight: 48)` (`56×56` for AI chat send), `iconSize: 26-28`, citation-chip vertical padding 14 dp, suggestion-chip font 14 pt; toolbar chip min height 44 dp. Extract `lib/widgets/chat_composer.dart` parameterised so the bump lands on both AI chat AND public chat together (currently two implementations).
- **Where**: `lib/screens/iso_notebook_screen.dart:4416-4446`; `lib/screens/ai_chat_screen.dart:209-214, 264-269, 332-356, 506-509, 581-591`; `lib/screens/chat_screen.dart:665-674`
- **Why**: 22-26 dp tap targets on nitrile/leather gloves = coin-toss; two Composers means P1-07 has to be applied twice with manual reconciliation.
- **Effort**: S
- **Refs**: round-1 [1, 19, 20, 44, 45]; round-2 Iter #19

#### P1-08 — Body / hint / timestamp font sizes for outdoor readability + shared SnackBar helper
- **What**: Chat body 13 → 15-16 pt w500; timestamps 10 → 12 pt with `_kTextSec`; composer hint 13 → 15; AppBar titles 18 → 20 w700; SnackBar duration 4s → 6-8s with floating + colored background. Extract `showRetrySnackBar(ctx, message, onRetry)` helper in `lib/widgets/snackbars.dart` (7 s + floating + `_kCard` + "Spróbuj ponownie") and replace all three current variants (ai_chat, chat, jobs).
- **Where**: `lib/screens/chat_screen.dart:273-274, 577-597, 652-660`; `lib/screens/ai_chat_screen.dart:108-122, 153-170, 318-325, 546-578`; `lib/screens/jobs_screen.dart:500-505`
- **Why**: Sunlight + tinted visor + sweat = 10-13 pt low-contrast text unreadable; 4 s default cannot be read with gloves.
- **Effort**: S
- **Refs**: round-1 [19, 20, 44, 45]; round-2 Iter #19, Iter #4

#### P1-09 — Replace `GestureDetector` with `InkWell` + haptic on every tappable row/chip
- **What**: Wrap pre_weld checklist rows, AI chat suggestion chips, _RoomTile, BOM list rows, elbow takeout cells in `Material + InkWell` with `BorderRadius.circular(10)`; add `HapticFeedback.selectionClick()` in onTap. Also add `Haptic.tap()` at the start of the jobs_screen Edit-IconButton try block (currently missing while Delete has it 4 lines below).
- **Where**: `lib/screens/pre_weld_checklist_screen.dart:311-372`; `lib/screens/ai_chat_screen.dart:494-512`; `lib/screens/material_list_screen.dart:134-154`; `lib/screens/jobs_screen.dart:483-507`
- **Effort**: S
- **Refs**: round-1 [11, 14, 15, 19, 36, 39]; round-2 Iter #4

#### P1-10 — Distinguish empty-from-filter from no-data + clear button (Jobs, Elbow, BOM, Help)
- **What**: When filter is non-empty render "Brak ogłoszeń dla \"{filter}\" — [Wyczyść filtr]" with action; add `suffixIcon` IconButton(Icons.clear) on every search TextField; elbow_takeout: detect `kElbowTakeouts.isEmpty` → distinct "Tabela kolan chwilowo niedostępna — sprawdź połączenie" with retry; help_screen empty state → add "Przeglądaj kategorie" TextButton; BOM detect missing-pid vs empty-segments.
- **Where**: `lib/screens/jobs_screen.dart:210-211`; `lib/screens/elbow_takeout_screen.dart:64-94`; `lib/screens/material_list_screen.dart:44-47`; `lib/screens/help_screen.dart:188-200`
- **Effort**: S
- **Refs**: round-1 [4, 12, 37, 39]; round-2 Iter #12, Iter #22, Iter #42, Iter #50

#### P1-11 — Webhook-polling banner during Stripe checkout + raise verification budget + dual-tap guard
- **What**: Set `_waitingForWebhook = true` inside the 12s poll loop, render top banner "Czekamy na potwierdzenie płatności…", disable Add FAB; lift `_kVerifyBudget` 15s → 30-45s; add LinearProgressIndicator on the verification overlay; add "Już zapłaciłem" re-poll button. Plus: remove either the outer GestureDetector OR the inner FilledButton on `_PlanCard` (dual tap targets); in-function guard `if (_creatingCheckout) return;` at the top of `_startCheckout`.
- **Where**: `lib/screens/jobs_screen.dart:75-111`; `lib/screens/premium_screen.dart:124, 460-530, 769-849, 537-547`
- **Why**: Direct revenue-disputes vector; cellular flake + tap-mash = duplicate Stripe sessions; basement signal makes legitimate webhook >15 s.
- **Effort**: M
- **Refs**: round-1 [4, 18, 43]; round-2 Iter #18

#### P1-12 — Tighten angle bounds + multiplier sanity warning on rolling offset
- **What**: Reject angle ≤ 5° or ≥ 89°; allow 90° as edge case (Run = 0); show yellow banner when `multiplier > 10`.
- **Where**: `lib/screens/rolling_offset_screen.dart:48, 67`
- **Effort**: S
- **Refs**: round-1 [7, 32]

#### P1-13 — Sanitise + length-cap free-text fields before clipboard/PDF/AI
- **What**: Add `maxLength` + `FilteringTextInputFormatter.deny(RegExp(r'[\r\n]'))` to project name (80), deduct name (24), AI-suggested componentId (24), tbLine, orbital `_trace` (40), saddle `_projectCtrl`, register email (80) + phone newline-strip. In orbital copy handler also `_trace.text.replaceAll(RegExp(r'\s+'), ' ').trim()`. Add regex email validator on register screen.
- **Where**: `lib/screens/iso_scanner_screen.dart:269-272, 1383-1391, 756-766, 1206-1209`; `lib/screens/orbital_tig_screen.dart:33, 153-165, 239-242, 250`; `lib/screens/saddle_template_screen.dart:204-208`; `lib/screens/register_screen.dart:85-92`; `lib/screens/job_add_screen.dart`
- **Effort**: S
- **Refs**: round-1 [2, 35]; round-2 Iter #5, Iter #6, Iter #10, Iter #24

#### P1-14 — Add `IconButton(Icons.help_outline)` to every calc AppBar + unify formula-dialog pattern
- **What**: Wire hydrotest, quick_converter, pre_weld, orbital_tig AppBars to existing `HelpButton` / `kHelp*`; pre_weld also link to weld_journal "save artifact" once `all == true`. Unify the two heat_input "explain result" patterns (one dialog, one tooltip) — extract `_showFormulaDialog(title, formulaText, variableTable, sourceCitation)` and use both tabs. Bump icon to 18 px in 44 px hit area.
- **Where**: `lib/screens/hydrotest_screen.dart:84-100`; `lib/screens/quick_converter_screen.dart:26-38`; `lib/screens/pre_weld_checklist_screen.dart:178-189`; `lib/screens/heat_input_screen.dart:323-335, 590-612`
- **Effort**: S
- **Refs**: round-1 [8, 11, 15, 33, 36, 40]; round-2 Iter #16

#### P1-15 — Show always-on safety / hold-time / step-by-step banners
- **What**: Pin BHP/Safety panel, "10 min minimum hold", purge / O₂ < 50 ppm / fit-up checklist ABOVE the inputs (not after result computation). Add zero-flow nudge on quick_converter Flow tab ("⚠ Sprawdź czy zawór otwarty" when `lpm == 0`).
- **Where**: `lib/screens/hydrotest_screen.dart:242-281`; `lib/screens/orbital_tig_screen.dart:277-303`; `lib/screens/quick_converter_screen.dart:425`
- **Effort**: S
- **Refs**: round-1 [8, 10, 33]; round-2 Iter #15

#### P1-16 — Per-process default V/I/travel + per-process η on heat input
- **What**: Set `'SAW': 1.00` per ISO/IEC 1011-1; split FCAW → FCAW-G (0.80) / FCAW-S (0.75); `_defaultParamsFor(process)` returning sensible (V, I, travel) tuples that swap when user changes process; display HI as `toStringAsFixed(2)`; WPS range with ±10% tolerance band; per-chip Tooltip with eta value. Add Snackbar+Undo when chemistry overwrite happens via `_applyMaterial`; toggle behaviour: tap selected chip → clear `_material` without touching chemistry; "i Wpisz własne" text-button next to chips.
- **Where**: `lib/screens/heat_input_screen.dart:13-18, 50-67, 74-87, 89-95, 343-372, 442-455, 888-934`
- **Effort**: S
- **Refs**: round-1 [16, 41]; round-2 Iter #16

#### P1-17 — Surface interpass + low-H electrode advice based on P-Number, not just CE + cross-tab discovery
- **What**: When `_material.pNumber >= 3` always show "Use low-H electrodes (E7018-1 H4R or equiv.)"; parse `preheatNote` into preheat + interpass max, render two metric cards; P-No-aware thickness threshold (P91 = 10 mm). Add a pinned summary chip strip ABOVE TabBarView showing the current material + live CE + live HI; long-press copies full trace; tap on each chip switches to relevant tab. Add hint under WPS range card on Tab 1 — "Tip: pick a grade on the Preheat tab to auto-fill". Add "TY TUTAJ" marker in CE reference table for the current CE bucket. Add P-No coverage for {3, 6, 42} in `_pNumberExtras` (or amber "Grade-specific checks not available — follow your WPS"); add unit test that every catalog `pNumber` exists in `_pNumberExtras` or an explicit allowlist.
- **Where**: `lib/screens/heat_input_screen.dart:140-150, 161, 163-209, 559-670, 674-684`; `lib/screens/pre_weld_checklist_screen.dart:27-86`
- **Effort**: M
- **Refs**: round-1 [16, 41]; round-2 Iter #16, Iter #41

#### P1-18 — Pin live CUT result to top of dimension dialog + collapse advanced options
- **What**: Move the live-result card (lines 1525-1615) to the TOP of `_askCalc` (above ISO input); collapse slope / DimRef / insulated controls under `ExpansionTile("Opcje zaawansowane")`; move "Usuń" to icon-only header button with second-tap confirm.
- **Where**: `lib/screens/iso_notebook_screen.dart:1318-1769`
- **Effort**: M
- **Refs**: round-1 [1]

#### P1-19 — Debounce text-field recompute + throttle Slider + suppress mid-typing cross-field errors
- **What**: Debounce `_recompute` ~150 ms via cancelable Timer on `onChanged`; Slider recompute on `onChangeEnd` or integer-degree boundaries; suppress validation errors while field empty (orbital "Ścianka > 1/2 OD" mid-typing). Suppress the cross-field "wall > OD/2" rule unless BOTH fields settled AND non-empty.
- **Where**: `lib/screens/saddle_template_screen.dart:60-92, 286-325`; `lib/screens/orbital_tig_screen.dart:50-77, 322`
- **Effort**: S
- **Refs**: round-1 [6, 10, 31, 35]; round-2 Iter #6, Iter #10

#### P1-20 — `RepaintBoundary` + cache painter / const colors + perf hygiene
- **What**: Wrap CustomPaint + MetricsRow in `RepaintBoundary`; cache `_CutProfilePainter` on `_template`; promote alpha-modulated colors to top-level `const` literals; remove file-wide `prefer_const_constructors` ignore (also on ai_chat_screen); mark `_TryChip` / `_Metric` / `_MessageBubble` / `_TypingIndicator` / `_CitationChip` constructors `const`. AI chat: hoist `_typing` to `ValueNotifier<bool>`, wrap bubbles in `RepaintBoundary` + `ValueKey`. Move `SaddleTemplate(...)` construction to `compute()` for `pointsCount >= 73` or memoise on `(h, b, angleDeg)`.
- **Where**: `lib/screens/saddle_template_screen.dart:1, 67-92, 239-241, 587-905`; `lib/screens/ai_chat_screen.dart:1, 196-206, 275-379, 433-458, 289-298, 304-313, 339-345, 365-374, 415-423, 449-451, 546-578`
- **Effort**: M
- **Refs**: round-1 [6, 31]; round-2 Iter #49

#### P1-21 — Add unit, joint-ID, batch, gas/polarity + save-to-log on tungsten + persistence
- **What**: Add optional joint ID / WPS ref / welder stamp + batch/lot + grind-angle chip selector above amps; "Save to job log" button writes {ts, jointId, amps, diaMm, typeCode, polarity, gas, batch, outOfBand} to existing job-log or `prefs_tungsten_history` JSON list capped at 20; render orange "POZA ZAKRESEM" tile when amps outside band; per-row long-press copy with PL/EN snackbar + haptic; fix overlap tie-break docs; persist `_amps.text` under `prefs_tungsten_amps` (debounced via addListener pattern) + restore in initState.
- **Where**: `lib/screens/tungsten_screen.dart:23-247, 24, 35-37, 58`; `lib/data/tungsten.dart:8-97`
- **Effort**: L
- **Refs**: round-1 [17, 42]; round-2 Iter #17

#### P1-22 — Persist `R`, last inputs, decimal-separator preference, invalidate stale results on input change
- **What**: Implement `WidgetsBindingObserver`; persist all 5 controller texts + R via `SharedPreferences` on success of `_calculate()` and on `paused`/`inactive`; restore in `initState()`; add `prefs_decimal_separator` (auto/dot/comma) + `prefs_route_decimals` (0/1/2); centralise `_saveSettings()` helper called from Reset-R / Undo paths. Add `onChanged: (_) => _invalidateResults()` to each `_field(...)` so input edits clear the three result fields (change empty-state guard to a `bool _hasResult` flag).
- **Where**: `lib/screens/pipe_route_calculator_screen.dart:15-118, 101-110, 129-141, 173-240`
- **Effort**: M
- **Refs**: round-1 [9, 34]; round-2 Iter #39

#### P1-23 — Extract palette to `lib/theme/app_colors.dart` + Scaffold backgroundColor + AppBar consistency
- **What**: Add `const _kBg = Color(0xFF0F1117);` and `backgroundColor: _kBg,` to pre_weld_checklist Scaffold; consolidate `_kCard / _kBorder / _kOrange / _kGreen / _kSec / _kMuted` from 26+ screens into shared `app_colors.dart` (semantic roles: `bgPrimary`, `accentPremium`, `accentUserBubble` — not raw hex); rename `_kSec` → `_kTextSec`, `_kMuted` → `_kTextMut`. Drop the file-private `_kAccentBlue = 0xFF4A9EFF` invented in ai_chat (already lives in elbow as `_kAccent`). Drop explicit `backgroundColor: _kCard` on ai_chat AppBar (inherit theme).
- **Where**: `lib/screens/pre_weld_checklist_screen.dart:7-12, 178`; `lib/screens/ai_chat_screen.dart:15-21, 145`; sweep across all 26 screens
- **Effort**: L
- **Refs**: round-1 [8, 11, 16, 35, 36, 43]; round-2 Iter #19

#### P1-24 — Switch every calc screen to one consistent material picker (`Wrap`, with tooltip)
- **What**: Extract shared `lib/widgets/material_picker.dart` exporting MaterialCatalog as `Wrap(spacing: 6, runSpacing: 6)` of ChoiceChips with long-press tooltip (preheat note + °F conversion + `P-No X · CE ≈ ...`); adopt in heat_input and pre_weld; move `_preheatFahrenheit` to `lib/utils/temperature.dart` and memoise per MaterialSpec.key; compile regex once as top-level `final _kPreheatNumRe = RegExp(r'(\d+)\s*°?\s*C');` to avoid per-build recompile; `int.tryParse` guard.
- **Where**: `lib/screens/pre_weld_checklist_screen.dart:117-127, 236-272, 253-259`; `lib/screens/heat_input_screen.dart:442-455`
- **Effort**: S
- **Refs**: round-1 [11, 36]; round-2 Iter #11, Iter #16, Iter #41

#### P1-25 — Pre-weld checklist persisted state by date / save-snapshot helper
- **What**: See P0-12 (escalated). Keep this slot pointing at: stable `_Check(id, pl, en)` IDs, version field for the catalog (`{checkId, version, languageAtTickTime}`), extract list to `lib/data/preweld_checks.dart`, show PL/EN badge in AppBar so welder + auditor see same language.
- **Where**: `lib/screens/pre_weld_checklist_screen.dart:14-18, 88-150, 184-188`
- **Effort**: M
- **Refs**: round-1 P1-25 (escalated to P0-12); round-2 Iter #41

#### P1-26 — Wire "Copy/Share report" + ShareResult feedback on every calc/checklist screen
- **What**: Add `IconButton(Icons.share)` (or `Icons.copy_all_outlined`) to AppBar of pre_weld, hydrotest, BOM, tungsten that builds a one-line trace string, uses existing `ClipboardHelper`/`share_plus` with `ShareResult.status` mapped to PL/EN snackbar; localise the cut-list `_buildTextSummary` clipboard payload (pass `BuildContext` and use `context.tr` for every literal — PL "LISTA CIĘĆ", "Materiał:", "Sztanga:", "Kerf:", "(zostaje: ...)"). On heat_input, mirror "Kopiuj wynik" under the CE/Preheat card with `CE x · Preheat y · material · note` payload and AppBar "Share both" concatenating HI + CE + Preheat. On help_screen, add `IconButton(Icons.copy_all_outlined)` to expanded entry answer + wrap in `SelectableText`.
- **Where**: `lib/screens/pre_weld_checklist_screen.dart:174`; `lib/screens/hydrotest_screen.dart:88-98`; `lib/screens/material_list_screen.dart`; `lib/screens/tungsten_screen.dart`; `lib/screens/cut_list_summary_screen.dart:78-93, 201-220`; `lib/screens/heat_input_screen.dart:389-405, 562-670`; `lib/screens/help_screen.dart:467-551`
- **Effort**: M
- **Refs**: round-1 [8, 11, 13, 14, 17, 33, 36, 38, 39, 42]; round-2 Iter #13, Iter #16, Iter #22

#### P1-27 — VAT-invoice line + free-trial badge + "Anuluj w każdej chwili" + refund + identity-aware Premium
- **What**: Footnote line 451 → "Faktura VAT na żądanie — napisz po zakupie" (or Fakturownia-automated); "Pierwsze 7 dni za darmo" badge on yearly plan (Stripe `trial_period_days`); promote "Anuluj w każdej chwili" to own 13 pt line; BLIK in EN copy; replace "MVP" leak; remove naked "Discard"/"Reset". Add three lines above legal footnote: "7 dni za darmo na planie rocznym", "Zwrot 100% w ciągu 14 dni — bez pytań", "Faktura VAT na żądanie" (bump font 11 → 12 pt _kTextSec). Add top banner state-machine: loading → "Sprawdzam Twój plan…", active → green "Masz Premium do {date}" + "Zarządzaj" → Stripe portal, free + first-time → prices, free + recently-canceled → "Reaktywuj poprzedni plan"; explicit "Mam już Premium — przywróć" TextButton at the bottom.
- **Where**: `lib/screens/premium_screen.dart:69-80, 222-534, 449-456, 451-452`; `lib/screens/job_add_screen.dart:131, 136, 421-427`; `lib/screens/pre_weld_checklist_screen.dart:184`
- **Effort**: S
- **Refs**: round-1 [5, 18, 30, 43]; round-2 Iter #18, Iter #48

#### P1-28 — Disable export buttons + "long-press to copy" + actionable PDF errors + re-entrancy guard
- **What**: Disable PDF / Share / CSV `IconButton`s when `_groups.isEmpty || _segments.isEmpty` (greyed-out) and on `_exporting == true`; "Przytrzymaj aby skopiować" hint under each result card; haptic on PDF tap; map `PathAccessException` / `FileSystemException` errno 13 to actionable PL/EN message. Branch the saddle export catch on exception type (`MissingPluginException`, `FileSystemException`, `PlatformException` from share) with distinct PL messages; clear temp file on share cancellation/failure; add Retry SnackBar action. Add `if (_exporting) return;` at top of `_exportPdf` (before any await); snapshot `_project`/`_segments` into locals to avoid stale-state in async; UUID suffix in temp file name to prevent same-millisecond collisions.
- **Where**: `lib/screens/cut_list_summary_screen.dart:78-93, 198, 255-279`; `lib/screens/quick_converter_screen.dart:81-90`; `lib/screens/hydrotest_screen.dart:213-241`; `lib/screens/saddle_template_screen.dart:199-224`; `lib/services/saddle_template.dart:124-143`
- **Effort**: S
- **Refs**: round-1 [8, 13, 15, 38, 40]; round-2 Iter #6, Iter #43

#### P1-29 — Tooltips + PL casing of "Wyczyść" / "Spróbuj ponownie" / acronym quoting / static-text reachability
- **What**: Replace pre_weld TextButton "Reset" with `IconButton(Icons.clear_all, tooltip: 'Wyczyść')`; PL SnackBarAction "Ponów" → "Spróbuj ponownie"; PL discard "Porzuć" → "Porzuć zmiany"; tooltip `pl: 'Dodaj "$tag" do wymagań'`; replace SS abbreviation in job-add hint with "stainless steel" / "INOX"; standardise AppBar title `'Nowe ogłoszenie pracy'`. Add `tooltip:` to `IconButton(Icons.close)` in help search; localise weld_journal `'W-001 lub SP-001-W005'` hint; widen weld_journal date picker to `now.year - 30` / `now.year + 5`. Add `Semantics(button: true, expanded:..., label:...)` on help entry rows; tooltip on help filter chip badge count; localise hardcoded `Text('CUT LIST')` AppBar title and `_lineTag` fallback in cut_list_summary (`context.tr(pl: 'LISTA CIĘĆ', en: 'CUT LIST')`).
- **Where**: `lib/screens/pre_weld_checklist_screen.dart:184-188`; `lib/screens/job_add_screen.dart:131, 217, 241, 256, 285, 334, 365-378, 406, 421-447`; `lib/screens/help_screen.dart:107-148, 122-130, 286-334, 467-551, 522-543, 585-617`; `lib/screens/weld_journal_screen.dart:702, 795-796`; `lib/screens/cut_list_summary_screen.dart:106, 320, 85-89`
- **Effort**: S
- **Refs**: round-1 [5, 11, 30, 36]; round-2 Iter #3, Iter #13, Iter #22

#### P1-30 — Per-field error + sanity bounds on every numeric input
- **What**: Per-field `errorText` for parse errors (not only out-of-range); reject negative V/I/travel with "Travel speed must be > 0" instead of silent 0; clamp arc voltage to 6-20 V with yellow warning; deduct vs parent ISO sanity (would-push-CUT-below-zero); volume sanity OD/wall. On orbital, OD (0, 1000) mm + wall (0, 25) mm with "Sprawdź wymiar"; drop the silent `_volts.text ?? 10` default (Iter #10 finding 5: surface a parse error or "Napięcie wymagane" hint instead). Saddle: strengthen `if (!h.isFinite || !b.isFinite || h <= 0 || b <= 0 || h > 10000 || b > 10000)`. Tungsten: surface invalid-input errorText + amber out-of-band warning (`a < pick.minA || a > pick.maxA`). Quick-converter: per-tab sanity ceiling (length 100 m, pressure 2000 bar, temp 5000 °C, flow 1000 l/min); reject negative bar/psi/lpm; extend abs-zero guard to `°C < -273.15 || °F < -459.67`; tighten parse regex to `^-?\d*[.,]?\d*$`; render exponential when `v.abs() < 0.001 && v != 0`; explicit "wartość zbyt duża" errorText; resolve identical cfh/scfh rows. Register screen: regex email validator; min 10-char password + bcrypt/scrypt/argon2id hashing + live strength meter.
- **Where**: `lib/screens/heat_input_screen.dart:97-98`; `lib/screens/orbital_tig_screen.dart:50-77, 47-48, 54-75, 56, 229`; `lib/screens/iso_scanner_screen.dart:75-83, 1402-1410`; `lib/screens/hydrotest_screen.dart:62-79`; `lib/screens/saddle_template_screen.dart:67-76`; `lib/screens/tungsten_screen.dart:35-36, 47-59, 73-109`; `lib/screens/quick_converter_screen.dart:53-56, 58-62, 162-176, 232-272, 341-355, 424-438, 457-461`; `lib/screens/register_screen.dart:85-101`
- **Effort**: M
- **Refs**: round-1 [2, 10, 16, 33, 35, 41]; round-2 Iter #10, Iter #15, Iter #17, Iter #24, Iter #45, Iter #47

### P1 net-new (round-2)

#### P1-31 — Tutor scope filter + diacritic normalisation + timeout + per-request seq
- See P0-10 (escalated). Slot kept for ToC continuity.

#### P1-32 — Auto-scroll to results / sticky result panel on every calc screen
- **What**: Wrap the results block in a `GlobalKey`, after `setState(() {})` in `_calculate()` call `Scrollable.ensureVisible(_resultsKey.currentContext!, alignment: 0.0, duration: 200ms)`. On tungsten, after `pick` changes, `WidgetsBinding.instance.addPostFrameCallback((_) => Scrollable.ensureVisible(matchingRow.currentContext!, alignment: 0.2))`. Wrap empty state in `SizedBox(height: ~340)` or skeleton result fields so the layout has stable height before/after CALCULATE (prevents the ~220 dp jump on pipe_route_calculator).
- **Where**: `lib/screens/pipe_route_calculator_screen.dart:170-195, 197-204`; `lib/screens/tungsten_screen.dart:43-46`
- **Why**: After CALCULATE the keyboard collapses and TOTAL renders off-screen on a 360 dp phone in gloves; on tungsten 5.5" landscape the keyboard hides rows 2-4. Both = "did the calc fire?" confusion.
- **Effort**: S
- **Refs**: round-2 Iter #9, Iter #17 (round-1 P2-15 mentioned auto-scroll only for hydrotest/orbital/heat_input)

#### P1-33 — Pipe-route input layout: stack on narrow screens + visual sketch + visual hierarchy
- **What**: Stack H1/H2 and X/Y vertically on screens < 380 dp via `LayoutBuilder`; shorten labels to plain "H1 (mm)" and drop redundant `suffixText: 'mm'`; render result fields as `Card` + bold display-large monospaced number (NOT outlined TextField) so input vs output is glance-distinct; move Formula info button OUT of the TOTAL Row to a 48×48 trailing IconButton; wrap each trailing note (Wzór, FW/SW) in `ExpansionTile` with `lightbulb_outline`. Extract dialog text to `lib/help/pipe_route_formula.dart` with explicit LR/SR/3D selector (1.0·DN / 1.5·DN / 3.0·DN) and ASME B16.9 citation; add `Flexible` around the title + `mainAxisSize: min` on the new TOTAL row to prevent RenderFlex overflow at large text scale.
- **Where**: `lib/screens/pipe_route_calculator_screen.dart:128-156, 159-167, 197-204, 218-231, 223-230, 243-260, 267-302`
- **Why**: 168 dp labelText "X – bieg poziomy 1" ellipsizes mid-edit at 12345; 24 dp dialog icon inside 32×32 next to TOTAL is the gloved mis-tap of the day; SR vs LR ratio confusion = 30+ mm per elbow.
- **Effort**: M
- **Refs**: round-2 Iter #9, Iter #39

#### P1-34 — BOM screen: aggregate KPI header + grouping/filter + project context
- **What**: Add sticky `Container` header below AppBar (or `SliverPersistentHeader`) showing 3 KPIs: total pipe metres, total fittings count, item count; subtitle "Projekt: ... · wygenerowano: HH:mm". Group `_items` by category (memoised) with section headers + per-category Suma length/qty; add `SliverAppBar.large` with search TextField + category Chip row toggles. AppBar title accepts `Column` with project name in `labelSmall`. Add AppBar.actions: refresh, share (FAB or AppBar), filter_list (toggles search); use `share_plus` to share generated PDF; bottom SafeArea Row "Kopiuj listę / Eksport PDF / Drukuj".
- **Where**: `lib/screens/material_list_screen.dart:75-78, 79-154, 134-154`
- **Why**: BOM has zero aggregate ("ile metrów rury DN50?" = scroll-and-count), zero grouping (PIPE interleaved with TEE/FLG), zero share/export. Welder screenshots the list to WhatsApp.
- **Effort**: L
- **Refs**: round-2 Iter #14, Iter #44

#### P1-35 — Two-line BOM row layout + tabular figures + dual units
- **What**: Replace bare `ListTile.title` with `Row` containing fixed-width 56-72px `Container` rendering `catLabel` chip-style (mono, bold, bordered, color-coded PIPE=blue / ELB90=orange / TEE=green / valve=red), `subtitle: Text(it.description, maxLines: 2, overflow: ellipsis)`. Right column 92 px with `textAlign: end` + `fontFeatures: [FontFeature.tabularFigures()]`. Use `NumberFormat.decimalPattern(localePl)` for length; render "1,500 m (1500 mm)" for parts ≤ 10 m; append PIPE-only "~ N szt. po 6 m + 0,4 m" hint. Extract `_BomRow extends StatelessWidget` const-constructable wrapped in `RepaintBoundary`; pre-compute catLabel/enUnit/length string once; promote amber-tint colors to const literals. Cache SharedPreferences instance via `late final Future<SharedPreferences> _prefs`; skip leading `setState(_loading=true)` on first build if `_items.isEmpty`; on refresh keep previous list rendered.
- **Where**: `lib/screens/material_list_screen.dart:38-62, 64-70, 134-154, 147-153, 75-133`
- **Why**: 168 dp wall of identical-weight text with truncated DN/SCH/grade; `12.345 m` dot-vs-comma confusion = order-of-magnitude error; itemBuilder re-evaluates `context.tr` per row per build.
- **Effort**: M
- **Refs**: round-2 Iter #14, Iter #44 (high × 3)

#### P1-36 — Localise welcome message in AI chat + DEMO indicator across all AI surfaces
- **What**: Move the hardcoded PL welcome message into `context.tr(pl:..., en:...)` and seed in `build()` or post-frame callback. Add a shared `kAiBackendAvailable` indicator (small chip in the AppBar) on every screen calling AI (iso_scanner AI suggestions, premium status verification), OR move DEMO into a global banner when flag is false.
- **Where**: `lib/screens/ai_chat_screen.dart:54-61, 153-170, 180-186`
- **Why**: EN-locale UK/offshore fitter opens AI Asystent for the first time, hits a wall of Polish text → bounces. iso_scanner AI surprises QA tester with hallucinated values without any DEMO indicator.
- **Effort**: S
- **Refs**: round-2 Iter #19

#### P1-37 — AI/tutor demo answers: dual units everywhere
- **What**: In `_demoReply` and the backend system prompt: dual-unit P91 PWHT (`× 2.4 min/mm wall (≈1 h per 25 mm / per inch)`); heat-input formula with units spelled out ("Travel in mm/min → HI in kJ/mm. For in/min × 25.4 first") and dual-unit P91 ceiling (1.0-2.5 kJ/mm + 25-64 kJ/in); bolt-torque dual-unit form including SMYS preload conversion; saddle/coping units gloss; standardise to "slpm" with bracketed scfh; split NACE hardness into HRC base-metal + HV10 weld+HAZ limits (NOT a conversion). Add `units` field to outgoing http body (`BackendConfig.preferredUnitSystem`); document in backend prompt that model MUST quote dual units. Localise citation snippets through a `formatUnits(text, system)` helper.
- **Where**: `lib/services/ai_chat_service.dart:80-95, 149-160, 163-167, 173-176, 178-188, 191-197`
- **Effort**: M
- **Refs**: round-2 Iter #28 (high × 3 + med × 5)

#### P1-38 — Cut-list i18n: localise CSV header convention + plural rules + line-tag stability
- **What**: Add a leading `# generated by FitterWelderPro v…` comment line on CSV (or localise headers per `context.tr`); centralise `pluralBars(int n, BuildContext)` returning `1 sztanga / 2-4 sztangi / 5+ sztang` (PL) and `1 bar / N bars` (EN); reuse across `_PipeGroup` header + `_GlobalSummary`. Pipe `materialGroup` through a `materialCodeFor(group)` helper that always returns the ASME short tag (`CS / SS / DSS / IN`) independent of UI locale. Refactor seven `context.tr(pl: 'Sztanga $x', en: 'Stock $x')` to template pattern + `.format()` extension (or pre-format outside tr) so DE/AT future locale doesn't freeze placeholder formatting.
- **Where**: `lib/screens/cut_list_summary_screen.dart:226, 325-328, 475-476, 501-503, 526-529, 546, 564-567, 622, 647-650, 663`
- **Effort**: M
- **Refs**: round-1 P3-04; round-2 Iter #13

#### P1-39 — Saddle: typed exception contract + initState locale race
- **What**: Define `SaddleTemplateException` enum (`branchTooLarge`, `angleOutOfRange`) thrown by `services/saddle_template.dart` with structured fields (`branchOd`, `headerOd`, `angle`); switch on enum in `_localizeError`. Add unit test asserting the enum cases are thrown for known bad inputs. Defer the first `_recompute` to a post-frame callback so `AppLanguageController` is hydrated.
- **Where**: `lib/screens/saddle_template_screen.dart:60-92, 95-97`; `lib/services/saddle_template.dart:82-89`
- **Why**: Brittle string-contains matching against thrown English messages — a single rephrase silently regresses PL localisation. InitState locale race renders English on first frame regardless of device locale.
- **Effort**: M
- **Refs**: round-2 Iter #6

#### P1-40 — Chat permissions: rules sheet + report categories + own-message delete + nickname gate
- **What**: On first room-enter (gated by `chat_terms_v1_accepted`), show a modal sheet listing 4-5 rules with "Akceptuję / Anuluj"; only set flag and proceed after accept; expose same sheet via "i" icon in room AppBar. Replace report AlertDialog with `showModalBottomSheet` listing 4-5 reason chips (Spam, Link, Wulgaryzm, PII, Inne) + optional text field; pass `reason` to `ChatService.report`; map backend "already-reported-by-this-device" → "Już zgłoszono — czekamy na innych". When `isMine == true`, long-press opens sheet with "Skopiuj tekst" + "Usuń wiadomość" (delete confirm), wired to new `ChatService.deleteMessage(id, deviceId)`. On first chat entry (gated by `chat_nickname_set`), force the nickname dialog; reject `^(admin|moderator|mod|fitterwelder|support)$/i` + min 3 visible chars + strip zero-width; show current nickname in room AppBar subtitle.
- **Where**: `lib/screens/chat_screen.dart:46-148, 104-148, 111-148, 378-438, 440-479, 482-528, 546-547`
- **Effort**: L (UI) + L (backend delete + categorised report)
- **Refs**: round-2 Iter #20 (high × 4 + med × 4)

#### P1-41 — Premium first-time UX: price-above-the-fold + real demo gate + handoff hint
- **What**: Move `_PlanCard` row directly under `_Hero` (or render compact "19 PLN/mc · 149 PLN/rok (-35%)" pill inside `_Hero`); demote feature list to below pricing. Wire a true demo: tapping the AI tile opens AiChatScreen with the sample prompt pre-loaded, allows ONE response, then inline upgrade banner ("Spodobało się? Odblokuj nielimitowane pytania → 19 PLN/mc"). Add debug-mode assert that PremiumGate is enforced before shipping. When `!stripeBackendLive`: top banner "Premium uruchamia się wkrótce — zostaw email…" with inline TextField + "Daj mi znać" button → `/api/fitter/waitlist`, OR change SnackBar to 7 s + "Powiadom mnie" toggling a SharedPreferences flag. Before `launchUrl`, show 2-3 s onboarding dialog: "Otworzymy Stripe w przeglądarce. Po płatności wróć…" with "Rozumiem" button. Wrap acronym words in tappable Tooltip ("WPS = Welding Procedure Specification — karta procesowa spawania") + "30+ norm: ASME, EN, AWS — po polsku" badge.
- **Where**: `lib/screens/premium_screen.dart:252-457, 255-351, 322-446, 355-418, 380-418, 449-456, 537-547, 583-587`
- **Effort**: M
- **Refs**: round-2 Iter #18

#### P1-42 — Chat polling: empty-room first-message + lifecycle pause + dup-timestamp + battery
- **What**: When `_messages.isEmpty`, call `_refresh()` instead of returning (or initialise `sinceIso` to `widget.room.createdAt`). Implement `WidgetsBindingObserver` and pause the 8s Timer on `AppLifecycleState.paused`; on resume do one immediate `_pollDelta` then restart periodic. Track `_lastSeenId` alongside `lastCreatedAt`; when burst-receiving identical timestamps send `(lastCreatedAt - 1ms)` and dedup by id. Add `physics: const AlwaysScrollableScrollPhysics()` to rooms ListView so RefreshIndicator works at zero items.
- **Where**: `lib/screens/chat_screen.dart:88, 316-353, 351-353, 351-367`
- **Why**: Empty room never picks up first message; idle polling drains battery on backgrounded phone; identical-timestamp burst posts get silently lost ("DON'T weld yet — pressure not relieved").
- **Effort**: M
- **Refs**: round-2 Iter #50

#### P1-43 — Chat content + identity edges: min message length + nickname guard + isMine race
- **What**: Require `text.length >= 2` in `_send` (reject single-character / pure-whitespace); require `res.length >= 2` (or 3) + reject pure-numeric / whitespace-only in nickname dialog; show inline `errorText` instead of dismissing. Guard `isMine` as `_myDeviceId.isNotEmpty && message.deviceId == _myDeviceId`. Show nickname char counter ("12/32") or `helperText`. Bubble: fallback to `'Anonim'`/`'anon'` when `nickname.isEmpty`; suppress 0-height empty Text. Clamp bubble `maxWidth: min(0.78 * width, 480)` + `max(160)`. Soft "Początek rozmowy — bądź pierwszy" when `_messages.length <= 1`; skip `animateTo` when `maxScrollExtent < 1`. Force word-wrap on long no-whitespace tokens (inject ZWSP every N chars >40).
- **Where**: `lib/screens/chat_screen.dart:111-148, 120-127, 142-144, 378-380, 327-349, 502-518, 512-516, 553-556, 573-584, 585-592, 608-618`
- **Effort**: M
- **Refs**: round-2 Iter #50

#### P1-44 — Cut list summary screen: load + export lifecycle hardening
- **What**: Wrap the final `setState` in `_load` with `if (!mounted) return;`; add `bool _loadInFlight = false;` guard (`if (_loadInFlight) return; try { ... } finally { _loadInFlight = false; }`); bump a `_loadGeneration` int and bail if not latest. Add `int _loadGen = 0; final gen = ++_loadGen;` pre-await; check `if (gen != _loadGen || !mounted) return;` before `setState`. Override `didUpdateWidget` to re-trigger `_load` when `widget.projectId` changes. Snapshot `_project`/`_segments` into local `final` vars at start of `_exportPdf`; pair the export future with `CancelableOperation` and cancel in `dispose()`. Single `_copyInFlight` guard reused by `_share` + `_copyCsv`; `hideCurrentSnackBar()` before show. Add `@override void dispose()` stub.
- **Where**: `lib/screens/cut_list_summary_screen.dart:30-41, 43-76, 78-93, 95-99, 255-279`
- **Effort**: M
- **Refs**: round-2 Iter #43

#### P1-45 — Backend config: central runtime knobs + offline-mode kill-switch + failover URL list
- **What**: Replace the four `const bool ... = true` flags with runtime getters consulting `connectivity_plus` + circuit-breaker (3 consecutive 5xx within 60s → false for 5 min). Keep const versions as `_FORCED_xxxBackendLive` for rollout gate. Add `static String get baseUrl => const String.fromEnvironment('FITTER_BACKEND_URL', defaultValue: '...');` + SharedPreferences offline-mode toggle. Add `static const List<String> baseUrlFallbacks` (jubilant-charm primary, PrzetargAI Railway secondary) + `api_client.dart` rotation on connection-refused / 502/503. Define `requestTimeout`, `chatPollInterval`, `chatPollBackoffMax`, `retryMax`, `jobsCacheTtl` constants and adopt in every consumer (exponential-backoff chat polling). Reconcile `jobsBackendLive=false` comment vs `jobs_service.dart` gating on `stripeBackendLive` (align or delete misleading flag) + actual SQLite fallback. Add `kDebugMode` localhost override.
- **Where**: `lib/config/backend_config.dart:20-21, 23-41, 60-67`; `lib/services/jobs_service.dart:20, 35`; `lib/services/iso_scanner_ai.dart:24`
- **Why**: Compile-time flags can't disable a backend call when network is down or Railway is degraded. The `jobsBackendLive` flag is currently dead config — flipping it has no effect. During Railway outages each module times out independently slowly. No URL failover even though sister Railway deployment exists. Misleading doc-comments will mislead the next reviewer at 3am during an RCA.
- **Effort**: L
- **Refs**: round-2 Iter #30 (high × 3 + med × 3)

#### P1-46 — ISO notebook a11y: axis-lock `toggled` semantics + canvas tappable Semantics + chip `selected`
- **What**: Wrap axis-lock IconButton in `Semantics(toggled: _axisLock, label: _tr('Blokada osi izometrycznej', ...))` + `SemanticsService.announce(...)` on toggle. Add `onTap`/`onLongPress` semantic actions to the canvas Semantics node and either move OUTSIDE GestureDetector or merge them. Wrap each toolbar chip in `Semantics(selected: sel, button: true, label: e.$3)`; wrap each `groupLabel(...)` in `Semantics(header: true, child: ...)`; add `Tooltip(message: e.$3)`. Replace `hintText: 'np. 76'` with `labelText: 'Długość (mm)'` on the ~10 dimension TextFields; wrap bulk-sheet rows in `Semantics(label: 'Segment ${i+1} z ${total}', textField: true, ...)`. Wrap AppBar title GestureDetector with `Semantics(button: true, label: ...)`; mark close InkWell in empty-state hint with `Semantics(button: true, label: ...)`; `SemanticsService.announce` after each state-changing SnackBar. `ExcludeSemantics` on decorative icons (4061 edit pencil, 4309 touch_app, popup menu icons).
- **Where**: `lib/screens/iso_notebook_screen.dart:4047-4064, 4068-4144, 4233-4272, 4290-4358, 4416-4446, 1406-7452 (TextFields), 3522-3541, 3593-3599, 4108-4119, 4061, 4198, 4309, 7541-7553`
- **Effort**: M
- **Refs**: round-2 Iter #31

---

## P2 — High impact, larger effort

#### P2-01 — Auto-draft form state to SharedPreferences (weld editor, pipe_route, tungsten, heat_input)
- See P1-21 (tungsten escalated); plus weld_journal editor, heat_input. Persist controllers keyed by screen+row id on `onChanged`; restore in `initState`; clear on successful save; survives crash/swipe-down/background-kill.
- **Where**: `lib/screens/weld_journal_screen.dart:573-604`; `lib/screens/pipe_route_calculator_screen.dart`; `lib/screens/heat_input_screen.dart:50-67`
- **Effort**: L
- **Refs**: round-1 [3, 9, 17, 34, 41, 42]; round-2 Iter #17

#### P2-02 — Energy-method + pulsed-process heat-input toggle
- Add a "Energy method (kJ direct)" toggle that lets welder enter the inverter's displayed kJ; add "Pulsed" toggle exposing Ipeak / Ibase / duty fields computing `I_eff = √(Ip² × duty + Ib² × (1-duty))`; document voltage-at-arc note. When HI is out of WPS range, add a one-line directional hint computed from current values ("Spróbuj zwiększyć prędkość spawania do ~X mm/min").
- **Where**: `lib/screens/heat_input_screen.dart:120-127, 367-372`
- **Effort**: L
- **Refs**: round-1 [16, 41]; round-2 Iter #16

#### P2-03 — Thickness + hydrogen-scale aware preheat (BS EN 1011-2 Method B)
- Yurioka / BS EN 1011-2 Method B lookup (CE × thickness × HI × hydrogen scale); H1/H2/H3/H4 chip selector defaulting H10 SMAW / H4 GMAW; return preheat as min-max range; °C ↔ °F toggle.
- **Where**: `lib/screens/heat_input_screen.dart:140-150`
- **Effort**: L
- **Refs**: round-1 [16, 41]

#### P2-04 — Fullscreen / hide-toolbar mode + dismissible color legend on ISO notebook
- Add `Icons.fullscreen` collapsing toolbar + summary into 40 dp strip; `showColorLegend` toggle in "Widok"; auto-hide legend after 10 segments; collapse AppBar's 9 actions into ≤4 primary + popup menu.
- **Where**: `lib/screens/iso_notebook_screen.dart:4066-4361, 4659-4723`
- **Effort**: M
- **Refs**: round-1 [1]

#### P2-05 — DN / SCH lookup table in hydrotest + heat_input (auto-fill OD + wall)
- "Pick standard pipe" link / `📋 DN/SCH` button above OD field opening picker DN15-DN300 × SCH10/40/80; auto-fills OD + wall; bar / MPa / psi toggle for design pressure with live "= X MPa" preview.
- **Where**: `lib/screens/hydrotest_screen.dart:101-178`
- **Effort**: M
- **Refs**: round-1 [8, 33]

#### P2-06 — Rotating sample prompts + Free vs Premium comparison table on Premium
- Rotate 2-3 sample AI prompts every few seconds; "Free | Premium" mini-table above feature tiles with green "NOWE" pill on PRO-only; per-month equivalent "ok. 12 zł/mc" under yearly; replace "270 KB" with human units ("30+ norm ASME/EN/AWS"); replace fish-mouth blurb with PL "rybi pyszczek"; move Bez-reklam tile to position 2; localise the "Coping & saddle templates" tile title to PL; fix `OSZCZĘDZASZ 35% · POPULARNE` → `OSZCZĘDŹ 35% · BESTSELLER`; period abbr `/mc` → `/mies.`; prices localised with conversion `19 PLN (~€4.50)` for EN.
- **Where**: `lib/screens/premium_screen.dart:233, 322-446, 367, 380-418, 399, 427-441, 451-452`
- **Effort**: M
- **Refs**: round-1 [18, 43]; round-2 Iter #48

#### P2-07 — Outdoor-mode (light-canvas + high-contrast) toggle + status visibility
- Global Brightness / outdoor-mode toggle in Settings (and canvas screens) switching to light canvas + brighter Scaffold `#15192A`; first-launch prompt "Pracujesz na zewnątrz?"; auto-suggest on screen-brightness ≥80%. Expose `PremiumService.isPendingDowngrade` + `downgradeGraceRemaining` getter for UI banner. Add `bool get isDebugOverride` field on PremiumStatus + red "DEBUG" badge. Plan labels ALL-CAPS tail ("PRO · MONTHLY"); replace U+00B7 with " — " or "/"; expose `Color get badgeBg` + `IconData get glyph` colour-blind-safe high-contrast.
- **Where**: `lib/screens/iso_notebook_screen.dart:586, 4039-4042`; `lib/screens/chat_screen.dart:69, 487`; `lib/services/premium_service.dart:39-91, 67-78, 153-162, 222, 225-289`
- **Effort**: L
- **Refs**: round-1 [1, 20, 45]; round-2 Iter #27

#### P2-08 — Pull-to-refresh + stale-data SnackBar + first-project celebration on Home
- Wrap ListView in `RefreshIndicator` calling `_load`; on refresh-after-stale-cache "Nie udało się odświeżyć — pokazuję zapisane dane" + Retry; on BOM refresh "Zaktualizowano BOM (N pozycji)". Track `_hasNonFiniteRows`. After `_load` if `_recent.length < _totalProjects` fire short SnackBar "Łącznie X projektów — pokazano 3 najnowsze" with "Zobacz wszystkie" action. After return from FitterMenuScreen, if `_totalProjects` incremented fire green SnackBar "Pierwszy projekt dodany — gotowy do CUT LIST". Language change confirmation SnackBar + on persist failure error toast with Retry. Adopt `_snack(BuildContext, {required pl, required en, action, bg})` helper in `lib/utils/snack.dart`.
- **Where**: `lib/screens/jobs_screen.dart:181-209`; `lib/screens/material_list_screen.dart:79-154`; `lib/screens/ai_chat_screen.dart:108-121`; `lib/screens/home_screen.dart:93-100, 130-132, 244-340, 259-322`
- **Effort**: M
- **Refs**: round-1 [4, 14, 39]; round-2 Iter #21

#### P2-09 — PDF accessibility + Polish-diacritic font + B&W variant + binder-margin + body-row legibility
- Add `pw.Document(title, author, subject, keywords, creator)` metadata + language tag; caption under embedded image; bundle Roboto-Mono for cut-list (drop Courier); `headerCount: 1` on BOM with caption; `pw.Bullet`; raise `#9BA3C7` → `#5A6280`; transliterate PL chars in `_safeFileName`; full-Polish error messages; print-friendly help variant (black-on-white, bilingual two-column, TOC). Bump cut-list rows 9 → 11 pt (Courier) and BOM cells 10 → 12 pt with `cellHeight: 20`. Bundle `Roboto-Bold.ttf` and wire all four weights properly (today every weight = Regular). Use `pw.EdgeInsets.only(left: 57, top: 28, right: 28, bottom: 28)` for binder hole-punch tolerance. Use `out.length.clamp(0, 40)` after the replace chain (not original length); trim leading/trailing `_`; keep diacritics with `[^\p{L}\p{N}_-]` Unicode regex. Drop path interpolation in StateError; throw typed `IsoPdfExportException` with enum cause (`emptyFile`, `noSpace`, `noPermission`). Add zebra striping + row-number badges to cut-list body. Define `TextTheme` with Polish-glyph-tested font (Inter / Roboto extended-Latin).
- **Where**: `lib/services/iso_pdf_export.dart:30-259, 43-50, 62, 88-108, 97-105, 126, 153-158, 241-246`; `lib/screens/help_screen.dart:24-695`; `lib/screens/cut_list_summary_screen.dart:14-20, 156-168, 321, 504, 559, 622, 641, 663-664`
- **Effort**: L
- **Refs**: round-1 [47, 49]; round-2 Iter #13, Iter #26

#### P2-10 — Persisted help-dismissed flag + favourites + first-launch orientation
- Add `help_seen_<screenKey>` set in `HelpButton` with "Don't show again"; for shared workshop tablets surface "training done" persistence; on first open of every calc add 1-line orientation header. Add `IconButton(Icons.star_border, tooltip: 'Dodaj do ulubionych')` in `_EntryRow` expanded section persisting to `SharedPreferences('help_favs')`; pin "Ulubione" chip at the head of filter strip pre-filtering pinned entries. Add `helperText: 'Synonimy: argon = purge'` to help search.
- **Where**: `lib/widgets/help_button.dart`; `lib/screens/hydrotest_screen.dart:101-104`; `lib/screens/orbital_tig_screen.dart:80-90`; `lib/screens/help_screen.dart:38-77, 107-148`
- **Effort**: M
- **Refs**: round-1 [8, 9, 10, 33, 34, 35]; round-2 Iter #22

#### P2-11 — Mixed-units support: per-project `originalUnit` + `formatLength()` helper
- Add `project.originalUnit` ('mm' | 'in') and `formatLength(value, unit, precision)` picking decimal precision per unit (mm→1, in→3) and decimal separator per `AppLanguage`; localise `49 PLN` to per-locale currency; split Segments KPI into mm/in buckets; introduce `MaterialSpec` with `densityKgPerM3` for downstream weight calc.
- **Where**: `lib/screens/home_screen.dart:155-307, 444-449, 697-710`
- **Effort**: L
- **Refs**: round-1 [21, 46]

#### P2-12 — Subscription undo / history / reactivation in PremiumService
- Bounded ring buffer (`List<PremiumStatus> _history`, max 8); `undoLastStatusChange()`; switch broadcast to `PremiumStatusChange{previous,next}`; expose `previousStatus`; `cancelCheckoutSession()` + `reactivateSubscription()`; cache pre-debug status with `debugRestorePreDebugStatus()`; replace `StreamController.broadcast()` with `BehaviorSubject.seeded(_current)`. On catch in `refreshFromBackend`, broadcast `_current` together with a `lastRefreshError` field (DateTime + reason) so widgets can render amber "ostatnia weryfikacja: 3 min temu" pill.
- **Where**: `lib/services/premium_service.dart:97-248, 225-289`
- **Effort**: M
- **Refs**: round-1 [50]; round-2 Iter #27

#### P2-13 — Calculation history + named presets persistence
- Append `{h1, h2, x, y, r, total, ts}` (and equivalents per calc) to JSON list in `SharedPreferences` capped at 20; "Recent" / "Bookmark" IconButton; for pipe_route persist named presets (`"Loop A - galley header"`).
- **Where**: `lib/screens/pipe_route_calculator_screen.dart`
- **Effort**: L
- **Refs**: round-1 [9, 17, 34, 42]

#### P2-14 — Per-row override-with-reason + cup/gas/grind angle for tungsten log + favourite
- Each electrode row tappable to override; prompt "override? reason:" and store {suggested, used, reason}; add `recommendedCup` + `argonFlow` to TungstenSize; tip-angle constrained per diameter; "in stock / preferred" shop flag (long-press). Long-press a type card → persist `prefs_tungsten_favourite_code` → reorder to top + pin icon on next open.
- **Where**: `lib/screens/tungsten_screen.dart:73-247, 33-37`; `lib/data/tungsten.dart:8-86`
- **Effort**: M
- **Refs**: round-1 [17, 42]; round-2 Iter #17

#### P2-15 — Empty-state coaching + skeleton loaders + auto-scroll-to-results (extend to tungsten)
- See P1-32 (auto-scroll part). Skeleton: replace centred `CircularProgressIndicator` with 3-4 shimmer skeleton job cards + label "Ładuję ogłoszenia…" with 6 s timeout prompt; auto-scroll to results card after focus loss on hydrotest / orbital / heat_input. On BOM (per P1-35) replace spinner with 6 shimmer ListTile + LinearProgressIndicator + "Buduje BOM…" caption + try/catch error path. On tungsten empty input render orange info banner "↑ Wpisz prąd (A) aby zobaczyć rekomendację".
- **Where**: `lib/screens/jobs_screen.dart:177-180`; `lib/screens/hydrotest_screen.dart:101-104`; `lib/screens/orbital_tig_screen.dart:87-90`; `lib/screens/material_list_screen.dart:38-78`; `lib/screens/tungsten_screen.dart:35-36, 73-109`
- **Effort**: M
- **Refs**: round-1 [4, 33, 35]; round-2 Iter #14, Iter #44, Iter #47

#### P2-16 — Accessibility live regions + screen-reader announcements
- Wrap validation error containers in `Semantics(liveRegion: true)`; `SemanticsService.announce` when tungsten pick changes diameter; `Semantics(label, button: true)` on every AppBar IconButton (matching tooltip text). See also P1-46 (iso_notebook a11y rollup).
- **Where**: `lib/screens/orbital_tig_screen.dart:175-185`; `lib/screens/tungsten_screen.dart:33-37`; `lib/screens/cut_list_summary_screen.dart:107-132`
- **Effort**: S
- **Refs**: round-1 [13, 35, 38, 42]

#### P2-17 — Cut-list aggregator: drop redundant `nestCutsToBars` recomputation
- `_GlobalSummary` should accept the pre-computed `_groupPlans` map (or derived `totalBars/totalCutMm/totalWasteMm` triple) from the parent; `_buildTextSummary` and `_buildCsv` should iterate `_groupPlans.entries` instead of re-calling `nestCutsToBars`. Kills the rotation-during-export concurrency race vector by making synchronous work O(segments) instead of O(segments × bar_packing).
- **Where**: `lib/screens/cut_list_summary_screen.dart:206-219, 228-251, 341-413`
- **Effort**: M
- **Refs**: round-2 Iter #43

#### P2-18 — Add GDPR data-control + chat coming-soon opt-in copy
- In Settings (linked from room-list AppBar overflow): "Usuń moją historię czatu" and "Pobierz moje wiadomości (JSON)"; both call dedicated backend endpoints keyed to deviceId. When `chatBackendLive == false` coming-soon screen says "Po włączeniu zostaniesz zapytany czy chcesz dołączyć — nic się nie wysyła automatycznie." + "Powiadom mnie" button that toggles SharedPreferences flag and triggers real opt-in prompt on first live load.
- **Where**: `lib/screens/chat_screen.dart:46-67, 151-189, 327-349`
- **Effort**: M (UI) + L (backend endpoints)
- **Refs**: round-2 Iter #20

---

## P3 — Polish / nice-to-have

#### P3-01 — Per-process tooltip + bilingual fixes on quick converter
- `helperText: 'Dot or comma'` + `hintText: '12,5'` to all 4 TextFields; wrap dropdowns in Tooltip with "Source unit"; wrap result `_Row` labels in Tooltip explaining slpm vs l/min; bump `_SmallHint` to 12-13 pt non-italic `_kSec`; icon-prefixed Tab labels; `Haptic.tap()` on every dropdown change. Render faint placeholder example card under `Opacity(0.4)` labelled "Przykład / Example" until `v != null`.
- **Where**: `lib/screens/quick_converter_screen.dart:26-465`
- **Effort**: S
- **Refs**: round-1 [15, 40]; round-2 Iter #15, Iter #45

#### P3-02 — Footer source citations + dual-unit thickness label on heat_input
- "AWS D1.1:2020 Annex H; for ASME B31.3 service consult Table 330.1.1" under CE reference table with clause refs per row; thickness label "Grubość [mm / in]"; consolidate P91 PWHT range to single source-of-truth; reword dialog citation "ISO/TR 17671-1 (η) + ASME IX QW-409.1 (HI definition)".
- **Where**: `lib/screens/heat_input_screen.dart:23-27, 678-695, 721-748`; `lib/data/material_catalog.dart:109`
- **Effort**: S
- **Refs**: round-1 [16, 41]

#### P3-03 — Match-input affordances + Tooltip on tungsten / elbow takeout / heat_input fields
- `inputFormatters: [FilteringTextInputFormatter.allow(RegExp(r'[0-9a-zA-Z/ ".dn]'))]` + sanitise zero-width / bidi marks on elbow filter; `closestByDn` jump-to behavior; `_Cell` clipboard payload includes "mm" suffix; PL/EN labels via `context.tr`; `Key(ValueKey(_rows[i].dn))` on ListView.builder; document `closestByDn` tie-break. Also long-press copy / share BOM per row (`InkWell(onLongPress: ClipboardHelper.copyWithToast)`). `_cachedRows = next; _cachedQ = _q;` atomic order in `_rows` getter; `ScrollController().jumpTo(0)` on `_q` non-empty change so the single matching row never renders below viewport. Empty state banner when `_rows.isEmpty && _q.isNotEmpty`. Guard `clipboard_helper.dart` `Clipboard.setData` in try/catch.
- **Where**: `lib/screens/elbow_takeout_screen.dart:11-212, 28-94`; `lib/screens/material_list_screen.dart:147-153`; `lib/data/elbow_takeouts.dart:51-62`; `lib/utils/clipboard_helper.dart`
- **Effort**: S
- **Refs**: round-1 [12, 14, 37, 39]; round-2 Iter #12, Iter #42

#### P3-04 — Standardise i18n style + casing utility across the app
- Drop `String.toUpperCase()` in `_SectionLabel.build` (or use Intl-aware upper); drop `final isPl = ...` inline reads in favor of `context.tr(pl, en)`; document `_chipCache` invariant; replace ellipsis `…` and em-dash `—` in hints with ASCII; standardise SnackBar punctuation rules in i18n style guide (see also P1-38 templated `tr` pattern).
- **Where**: `lib/screens/job_add_screen.dart:476-485`; `lib/screens/pre_weld_checklist_screen.dart:175, 354`; `lib/screens/saddle_template_screen.dart`; project-wide
- **Effort**: S
- **Refs**: round-1 [5, 30, 36]

#### P3-05 — Polish detail screens: status badge on Premium, EN BLIK mention, FittedBox badge, hero hierarchy
- Render green "Aktywne · do {date}" at top of Premium when `PremiumService.status.isActive` with "Zarządzaj subskrypcją" → Stripe portal; wrap "OSZCZĘDZASZ 35% · POPULARNE" in `FittedBox(BoxFit.scaleDown)` or maxLines: 2; SnackBarAction Retry on null-URL path; cap verification overlay maxWidth 360; replace AppBar gradient with solid `_kGold` for outdoor legibility; rotate Hero subtitle through concrete benefits; raise sample-prompt to 12-13 pt non-italic.
- **Where**: `lib/screens/premium_screen.dart:226-241, 333-343, 426-446, 460-547, 567, 632-675`
- **Effort**: M
- **Refs**: round-1 [18, 43]

#### P3-06 — Drop dead helpers, add data-version key + schema validation on static data files
- Replace cache key on elbow_takeout with `(query, kElbowTakeouts.length)` to invalidate when data changes; return `List.unmodifiable(...)`; `assert` on monotonic DN + uniqueness; `static const int schemaVersion = 1`; wire `closestByDn` to "jump-to DN" or unit-test until used. Defensive `if (kTungstenSizes.isEmpty) return null;` in `sizeForCurrent`; in tungsten screen render fallback `Text('Brak danych — zaktualizuj aplikację')` when `kTungstenTypes.isEmpty`.
- **Where**: `lib/data/elbow_takeouts.dart`; `lib/screens/elbow_takeout_screen.dart:23-50`; `lib/data/tungsten.dart:90-97`; `lib/screens/tungsten_screen.dart:171-242`
- **Effort**: S
- **Refs**: round-1 [12, 37]; round-2 Iter #47

#### P3-07 — Footer / hero copy hygiene + greetings
- Drop ellipsis on description hint; replace em-dash `—` with hyphen `-`; cycle greeting Spawaczu/Monterze/Fachowcu; "Discard changes?" → "Odrzucić wpisane dane?"; "Wróć do edycji" → "Anuluj"; footer "...pojawią się" → "...mogą pojawić się".
- **Where**: `lib/screens/job_add_screen.dart:117-460`; `lib/screens/home_screen.dart:402-418, 303-307`
- **Effort**: S
- **Refs**: round-1 [5, 21, 30, 46]

#### P3-08 — Section divider widget + AppRow style helper
- Extract `_SectionDivider({required String label})` to `lib/widgets/section_divider.dart`; standardise 10 dp border radius and "done / selected" treatment in `AppRowStyle`; min-text size 11 pt for inline P-No prefix.
- **Where**: `lib/screens/pre_weld_checklist_screen.dart:283-310`; project-wide
- **Effort**: M
- **Refs**: round-1 [11, 36]

#### P3-09 — Memoize step-hint + single Builder context on saddle_template + first-time coaching gate
- Compute `stepMm` / `stepDeg` once in `_recompute`; drop inner `Builder` wrapper; combine two for-loops over `template.points`; mark `_NumField` / `_Metric` / `_TryChip` constructors `const`; gate first-time coaching callout behind `prefs_seen` flag. Add `if (_exporting) return;` (see P1-28). Use UUID v4 / `Random().nextInt(1<<32)` suffix on temp file name to prevent millisecond collisions.
- **Where**: `lib/screens/saddle_template_screen.dart:269-422, 524-985, 199-224`
- **Effort**: S
- **Refs**: round-1 [6, 31]; round-2 Iter #6

#### P3-10 — Per-row long-press copy + share BOM (covered in P1-26 and P3-03)
- Slot kept as a stub for traceability with round-1 numbering.
- **Refs**: round-1 [14, 39]

#### P3-11 — Clipboard / share consent + temp-file cleanup + permission policy comment
- One-shot dialog before first session-copy: "Skopiujemy listę do schowka…" with "Nie pokazuj więcej" (persist via `SharedPreferences`); after share success delete temp PDFs or prune files >7 days; add top-of-file comment locking in zero-permission posture.
- **Where**: `lib/screens/cut_list_summary_screen.dart:78-93, 255-279`
- **Effort**: S
- **Refs**: round-1 [13, 38]

#### P3-12 — Dim help info-icon after first viewing; rolling-offset isDirty fixes
- Persist `prefs_pipe_route_formula_seen` and dim `info_outline` 40% opacity once true; reconcile `_isDirty` rules with the P0-05 wipe (see P0r-05 / P0r-06 — coordinate before either fix). Gate `_isDirty` on `_selectedAngle == 'custom' && _customAngleController.text.isNotEmpty`; include `_trueOffsetController.text.isNotEmpty` in `_isDirty`; auto-clear `_customAngleController` when selection changes away from 'custom'; allow rise OR spread = 0 (with at least one > 0); distinguish "empty/zero" from "unparseable" parse errors; wire `textInputAction.done` + `onSubmitted` on custom-angle field; `toStringAsFixed(2)` for sub-1 mm. Mark "Porzuć" as destructive (`foregroundColor: error`). Wrap `await showDialog` in try/catch returning false.
- **Where**: `lib/screens/rolling_offset_screen.dart:28-265, 99-126, 144-149`; `lib/screens/pipe_route_calculator_screen.dart:267-302`
- **Effort**: S
- **Refs**: round-1 [7, 9, 32, 34]; round-2 Iter #7, Iter #37

#### P3-13 — Polish detail on chat / outdoor visibility + nickname guard
- Polish chat chevron color, bubble border alpha 1.0, own-bubble alpha 0.45-0.55 or solid `0xFF6A3A85`, edit-nickname icon brighter, more_vert report on cudzy dymek, polling loading dot in AppBar / "last updated Xs ago" subtitle, _Composer font 16. Reject case-insensitive `{admin, moderator, mod, support, wsparcie, fitterwelder, fitter welder pro}` as nicknames; require ≥3 visible characters; strip zero-width; show live preview bubble in the dialog before committing. Always show date prefix in `_formatTime` when date differs from `now` regardless of same-day-next-week coincidence (or render relative time `2h ago`/`wczoraj 23:59`).
- **Where**: `lib/screens/chat_screen.dart:75-674, 111-148, 608-618`
- **Effort**: S
- **Refs**: round-1 [20, 45]; round-2 Iter #20, Iter #50

#### P3-14 — Static-text reachability: Polish diacritics + StateError messages in PL
- "sprawdz" → "sprawdź" in user-facing PDF-error text; translate iso_pdf_export `StateError` messages to PL when surfacing in UI; fall back to `'ISO z ${stamp.split(" ").first}'` when projectName empty; expand `Szt.` to `Sztuk`.
- **Where**: `lib/services/iso_pdf_export.dart:33, 154, 161-163, 252-259`
- **Effort**: S
- **Refs**: round-1 [49]

#### P3-15 — Drop unused dead-helper / latent untested paths (covered in P3-06)
- Either wire `closestByDn` to "jump-to DN" or unit-test until used; document or remove `_chipCache` single-language assumption. Slot kept for ToC continuity.
- **Refs**: round-1 [12, 37]

#### P3-16 — Add "Live region" + screen-reader announcements to dynamic UI changes (covered in P2-16, P1-46)
- Slot kept for ToC continuity.
- **Refs**: round-1 [30, 35, 42]

#### P3-17 — Hydrotest concurrency hygiene + report snapshot confirmation
- Track `bool _copying = false;` and gate `_copyReport` entry; gray the icon while in flight ~150 ms. For factor chips the existing `if (!selected) Haptic.tap();` already short-circuits redundant haptic — fine. Delete (or move after the await) the dead `if (!mounted) return;` BEFORE `await copyToClipboard` (the helper self-protects). Recompute the snapshot INSIDE `_copyReport` by reading controllers fresh OR show a confirmation snackbar listing captured test pressure + volume so a wrong-snapshot copy is immediately visible.
- **Where**: `lib/screens/hydrotest_screen.dart:51-79, 91-98, 340-342, 404-408`
- **Effort**: S
- **Refs**: round-2 Iter #8

#### P3-18 — Pre-weld haptic polish + frame-budget perf
- Move `Haptic.tap()` OUTSIDE the `setState` closure (call after `setState(...)` returns), mirroring `Haptic.saved()` placement at line 161 — defensive against future Haptic refactors. Add `bool _wasAllDone = false;` rising-edge guard for `Haptic.saved()`; or `Future.delayed(80 ms)` to avoid motor-cooldown collision with `Haptic.tap()`. Skip `Haptic.tap()` on toggles crossing the "all done" threshold; fire only `Haptic.saved()`. Compile `_kPreheatNumRe` regex once; memoise `_preheatFahrenheit` per MaterialSpec.key; `int.tryParse` guard (already covered in P1-24 — duplicate slot for backlog continuity).
- **Where**: `lib/screens/pre_weld_checklist_screen.dart:152-162, 117-127, 158, 161`
- **Effort**: S
- **Refs**: round-2 Iter #11

#### P3-19 — Heat-input + saddle async-crash hygiene (preventive)
- Wrap `copyToClipboard` in heat_input in try/catch + bool return path → red SnackBar on false. Either `await showDialog<void>(...)` for `_showHeatInputFormulaDialog` and gate post-dialog work with `if (!mounted) return;`, or document `unawaited(...)` at the call-site. Add marker comment + `mounted` guard in `_applyMaterial` so future async-fetch chemistry doesn't crash on dispose.
- **Where**: `lib/screens/heat_input_screen.dart:74-87, 389-396, 701-769`
- **Effort**: S
- **Refs**: round-2 Iter #46

#### P3-20 — AI chat micro-perf polish (suggestion strip + composer borders + citations)
- Cache `suggestions` in StatefulWidget initState (or memoise on language change) — avoid re-allocating the 5-entry list every rebuild; pull strings to `app_strings.dart` and make list `static const`. Hoist the three `OutlineInputBorder` instances + `InputDecoration` to file-level `final` (or `static final` inside `_Composer`). Extract `const _CitationChip({required String text})` so identical citations reuse Element identity.
- **Where**: `lib/screens/ai_chat_screen.dart:328-357, 475-481, 546-578`
- **Effort**: S
- **Refs**: round-2 Iter #49

#### P3-21 — Premium screen i18n micro polish
- Wrap PREMIUM AppBar title literal in `context.tr` (idempotent today but unblocks future).
- **Where**: `lib/screens/premium_screen.dart:233`
- **Effort**: S
- **Refs**: round-2 Iter #48

---

## Items dropped vs round 1
None. Every round-1 item is still applicable post-d025247 or has been escalated into a P0 regression-risk subsection (P0-05 → P0r-05/06; P1-25 → P0-12). P0-01..P0-05 are listed in the "Shipped today" section with strikethrough.

## Counts
- ✓ Shipped today: 5 (all round-1 P0)
- P0 regression-risk: 12 (P0r-01..P0r-12)
- P0 net-new: 9 (P0-06..P0-14, plus P1-25 escalated to P0-12)
- P1 still-open from round 1: 30 (P1-01..P1-30)
- P1 net-new from round 2: 16 (P1-31..P1-46) — P1-31 is a placeholder ref to P0-10
- P2 still-open from round 1: 16 (P2-01..P2-16)
- P2 net-new from round 2: 2 (P2-17, P2-18)
- P3 still-open from round 1: 16 (P3-01..P3-16)
- P3 net-new from round 2: 5 (P3-17..P3-21)
- Round-1 items dropped: 0
