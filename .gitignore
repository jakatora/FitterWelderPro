# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Windows build artifacts
/windows/build/

# Sensitive signing keys — NEVER commit
*.jks
*.keystore

# Large binary/archive files
*.zip
*.exe

# Local build logs
build_output*.txt
run_windows_log*.txt
analyze_output*.txt
analyze_after_copy_fix.txt
fix_analyze.txt
build_release_after_copy.txt
orchestrator_smoke_test_report.txt

# Replit export artifacts
replit_export/

# VS Code (optional — uncomment if you want to exclude)
# .vscode/

# Claude agent worktrees (tooling metadata)
.claude/

# Audit working files (raw — large, local-only). The prioritised
# BACKLOG_*.md files ARE tracked since they're the audit deliverable.
.audit/
audit_shots/
BACKEND_DEPLOY_REPORT.md
LOOP_PROGRESS.md
MORNING_TEST_PLAN.md
NIGHT_SHIFT_REPORT.md

# Separate backend repo cloned in here as a sibling — never embed.
/backend/

# Generated PNG exports from promo_assets/convert_to_png.ps1 (source SVG is
# tracked; raster outputs are derived and would bloat the repo).
/promo_assets/png/
