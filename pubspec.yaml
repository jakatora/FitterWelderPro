name: cut_list_app
description: "Fitter Welder Pro — cut list builder, ISO parser, pipe-fitting and welding calculators for industrial pipe construction."
publish_to: "none"
version: 0.1.6+12

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  sqflite: ^2.3.0
  sqflite_common_ffi: ^2.3.3
  path: ^1.9.0
  path_provider: ^2.1.2
  uuid: ^4.3.3
  intl: ^0.20.2
  file_picker: ^8.0.6
  image_picker: ^1.1.2
  http: ^1.2.2
  crypto: ^3.0.6
  pdf: ^3.10.7
  share_plus: ^9.0.0
  url_launcher: ^6.3.0
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
    - assets/fonts/Roboto-Regular.ttf
